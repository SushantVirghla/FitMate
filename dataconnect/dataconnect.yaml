specVersion: "v1beta"
serviceId: "todoList"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "todoList-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
